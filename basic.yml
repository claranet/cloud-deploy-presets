description: Common features needed by Ghost
features:
  -
    name: pkg
    value: package=dbus
    provisioner: salt
  -
    name: zabbix.agent
    value: ${GHOST_CLIENT}-${GHOST_ENV}-${GHOST_APP}
    provisioner: salt
